# OpenCodex 界面设计稿

模式：imagegen 内置工具，generate，ui-mockup，仅供预览。实现不引用生成位图，而是使用 Vue/CSS 原生控件。

生成图：`/Users/dalong/.codex/generated_images/01a07a93-fa37-7072-9db6-8cff444dcae7/exec-c8a456ef-a162-4867-85ae-91ca7b0c019f.png`

最终提示词：

```text
Use case: ui-mockup. Preview-only high fidelity macOS developer app redesign, OpenCodex Manager. Show two aligned app panels side by side: left "版本管理", right "数据传输". Chinese interface. White cards on very light neutral gray canvas, restrained blue #2563eb primary accent, dark slate text, subtle gray borders, no teal, no gradients, no giant warning banners. Compact 8px spacing system, 36px controls, all labels and baselines carefully aligned. Both have compact header title and current instance selector "work", secondary storage path, simple navigation tabs. Left: three compact version metrics; latest Engine v2.48.0 row with right-aligned update button; installed version rows v2.45.0 current, v2.42.0, v2.40.0 with consistent status and switch/delete columns; release dropdown/footer. Right: source instance "默认实例" and target "work" in equally sized cards connected by arrow; two equal radio option cards "复制合并" and "覆盖数据", labels inline with radio dots, descriptive second line; inline checkbox "包含用量历史"; subtle neutral safety note "自动备份 · 独立目录与端口"; preview table rows accounts, channels, models; aligned footer with secondary "预览传输" and blue "执行传输". Calm dense professional desktop UI, legible typography, no decorative illustrations. No legacy migration messaging. Not a wireframe, polished realistic UI screenshot.
```
